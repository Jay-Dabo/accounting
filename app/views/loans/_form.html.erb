<%= bootstrap_form_for([@firm, @loan]) do |f| %>
  <%= render 'shared/error_messages', object: @loan %>
  
  <div class="well row">
    <%= f.hidden_field :type, value: params[:type] %>   

    <div class="col-md-3">
    </div>
    <div class="col-md-2">
      <%= f.date_field :date_granted, label: "Tanggal", required: true %>
    </div>
    <div class="col-md-3">
      <%= f.text_field :amount, label: "Nilai", required: true, data: {autonumeric: {aSign: 'Rp ', aSep: '.', aDec: ',', mDec: 0}} %>
    </div>
    <div class="col-md-4"> 
        <%= f.text_field :contributor, required: true, label: "Sumber", placeholder: "nama orang, bank, atau lembaga lainnya" %>
    </div>
  </div>

  <div class="well row" id="loan-details">
    <div class="col-md-4">
      <%= f.number_field :interest, label: "Bunga" %>
    </div>    
    <div class="col-md-4">
      <%= f.date_field :maturity, label: "Jatuh Tempo" %>
    </div>    
  </div>

  <div class="actions">
    <%= f.submit "Simpan", class: "btn btn-primary" %>
  </div>
<% end %>
