<% title(@post.title) %>

<div class="row">
  <div class="col-sm-3"><h3><small><i class="fa fa-calendar"></i>&nbsp;&nbsp;<%= @post.updated_at.strftime("%Y, %B %-d") %></small></h3></div>
  <div class="col-sm-9"><%= sanitize(@post.content_html, length: 250, escape: false) %></div>
</div>
